import React from 'react';
import { Box, Typography } from '@mui/material';

const AssignTeam = () => {
  return (
    <Box>
      <Typography variant="body1">This is the Assign Team tab content.</Typography>
    </Box>
  );
};

export default AssignTeam;